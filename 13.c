#include<stdio.h>
#include<conio.h>
void main()
{
    int n,i,s;
    printf("enter n value");
    scanf("%d",&n);
    for(i=2;i<=n/2;++i)
    {
        if(n%i=0)
        {
           if(s=0)
           {
            printf("the number is prime");
        }
        else
        {
            
            printf("the number is not prime");
    
        }
    }
    getch();
}
}
